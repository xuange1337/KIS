# Production readiness

Дата аудита: 2026-09-16. Второй срез исправлений: 2026-09-18.

## Проверенная база

| Проверка | Результат |
|---|---|
| Shared build | успешно |
| Web unit/component tests | 32/32 |
| Web production build | успешно |
| API production build | успешно |
| API e2e после сессий | 87/87 |
| API e2e после срезов 2026-09-18 | 129/129 |
| Миграция организаций на непустой базе | 20 клиентов и 32 сделки перенесены, записей без организации нет |
| Откат миграции организаций | выполнен на `crm_test` без ошибок |
| `docs/schema.sql` | исполняется в PostgreSQL 17 без ошибок |
| `npm run ci` 2026-09-19 | проходит полностью: формат, линтер (0 ошибок), сборки, бюджет бандла, 32 web, 130 API, лицензии, секреты, compose |
| Резервная копия и восстановление | копия снята с рабочих контейнеров, восстановлена в отдельную базу: 1 организация, 4 пользователя, 20 клиентов, 32 сделки, версия схемы совпала |
| Повреждённая копия | отклонена по контрольным суммам |
| Шифрованная копия | снята и восстановлена с парольной фразой |
| `npm run smoke` | образы собраны, готовность `schema: current`, вход, отчёты, единый формат ошибки, OpenAPI, SPA |
| Бюджет бандла | крупнейший чанк 177 КБ gzip из 200, весь JS 536 КБ из 700 |
| Миграционный тест | миграции применены к базе с данными старой схемы, записи перенесены, откат последней миграции проверен |
| Нагрузочный smoke | 10 клиентов, 200 запросов: 443 запроса/с, p95 44 мс, ошибок нет |
| Первый прогон CI на сервере | нашёл две настоящие ошибки: секреты в самом workflow и падение smoke на чистой базе без учётных записей |
| Второй прогон CI | задание образов и smoke пройдено; в проверках выявлен тест, зависящий от времени суток |
| Третий прогон CI | пройден полностью, оба задания |
| Тёмная схема | проверена в браузере на дашборде, списке клиентов, отчётах и календаре; переключатель и сохранение выбора работают |
| Интерфейс на 360 px | проверен в браузере: меню в панели, содержимое во всю ширину, горизонтальной прокрутки страницы нет |
| Защита несохранённых изменений | проверена в собранном интерфейсе: подтверждение появляется, «Отмена» сохраняет введённое, «Закрыть» отбрасывает, клиент не создаётся |
| Docker Compose config | успешно |
| Docker build и container smoke | успешно, API/web healthy, login/refresh 200 |
| Docker smoke 2026-09-18 | API/web healthy, login 200, `/api/docs/openapi.json` 200, ошибка в едином формате, логи построчным JSON |
| Проверка лицензий 2026-09-18 | 515 production-пакетов, запрещённых лицензий нет |
| Production dependency audit | 0 critical, 0 high, 2 moderate |
| Исходный JS bundle | 1,75 МБ, gzip 513 КБ |
| JS bundle после route splitting | основной 560 КБ, gzip 181 КБ; страницы вынесены отдельно |

## Исправления от 2026-09-18

| ID | Дефект | Приоритет | Исправление |
|---|---|---|---|
| H-01 | Администратор мог заблокировать себя или снять с себя роль; восстановления пароля нет, и система оставалась без управления | P0 | `users.service.ts`: запрет самоблокировки и самопонижения, проверка «последний действующий администратор» |
| H-02 | Выход, «завершить все сессии», смена пароля и блокировка отзывали только refresh-токен: выданный access-токен работал до конца своего срока | P0 | `jwt.strategy.ts`: access-токен проверяется против живой сессии |
| H-03 | Повторное использование refresh-токена отзывало только одну сессию, хотя означает компрометацию учётной записи | P1 | `auth.service.ts`: отзыв всех сессий пользователя |
| H-04 | CSRF-cookie выдавалась только при входе и истекала раньше продлеваемой сессии: обновление начинало отвечать 403 | P1 | `auth.controller.ts`: cookie продлевается вместе с refresh-токеном |
| H-05 | Сортировка списков по неуникальному полю давала неустойчивый порядок: запись попадала на две страницы подряд или не попадала никуда | P1 | `paginate.ts`: вторичная сортировка по первичному ключу |
| H-06 | Журнал действий хранил свободный текст и строку запроса целиком — персональные данные жили в нём дольше самих записей | P1 | `audit.interceptor.ts`: маскирование, отсев секретов, ограничение длины значений |
| H-07 | По времени ответа формы входа определялось, существует ли логин | P2 | `auth.service.ts`: хеш сверяется и для несуществующего логина |

Регрессии закреплены в `server/test/hardening.e2e-spec.ts`.

## Данные, восстановление и выпуск от 2026-09-19

| ID | Что сделано | Приоритет | Где |
|---|---|---|---|
| R-01 | Копия базы, вложений и версии схемы с контрольными суммами, ротацией и опциональным шифрованием AES-256 | P0 | `scripts/backup.sh` |
| R-02 | Восстановление с проверкой целостности, числа записей и совпадения версии схемы; по умолчанию — в отдельную базу, а не поверх рабочей | P0 | `scripts/restore.sh` |
| R-03 | Runbook: RPO/RTO, расписание копий и еженедельной проверки, порядок действий при аварии, полной потере хоста и неудачном обновлении | P0 | `docs/runbook-disaster-recovery.md` |
| R-04 | Production smoke: сборка образов, запуск, миграции, вход, отчёты, формат ошибки, OpenAPI, SPA; режим чистых томов | P0 | `scripts/smoke.sh` |
| Q-01 | Единая команда проверки `npm run ci` | P1 | `package.json` |
| Q-02 | ESLint 9 и Prettier, кодовая база отформатирована | P1 | `eslint.config.mjs`, `.prettierrc.json` |
| Q-03 | Проверка секретов в отслеживаемых файлах | P1 | `scripts/check-secrets.mjs` |
| Q-04 | Бюджет размера бандла по сжатому размеру | P1 | `scripts/check-bundle-size.mjs` |
| Q-05 | `CHANGELOG.md` и чек-лист выпуска | P1 | `docs/release-checklist.md` |
| Q-06 | Пароль базы данных стал обязательным: значения по умолчанию в исходниках больше нет | P1 | `configuration.ts`, `docker-compose.yml` |
| Q-07 | Optimistic locking клиентов и сделок: одновременная правка не затирается молча, а отклоняется с 409 и актуальной версией | P1 | `common/helpers/optimistic-lock.ts`, миграция `AddRecordVersion` |
| Q-08 | Миграционный тест с предыдущей схемы: миграции применяются к базе с данными, данные переживают обновление, откат проверяется | P1 | `server/test/migration.e2e-spec.ts` |
| Q-09 | Нагрузочный smoke: параллельные клиенты, бюджет 95-го перцентиля | P1 | `scripts/load-test.mjs` |
| Q-12 | Версия в пути `/api/v1/...`; прежние пути без версии продолжают работать | P1 | `common/app-setup.ts` |
| Q-13 | Диагностика администратора: состояние экземпляра, схемы и базы, показатели данных в пределах организации | P1 | `diagnostics/` |
| Q-18 | Светлая и тёмная схемы: семантические токены на CSS-переменных, системная настройка по умолчанию, сохраняемый выбор, переключатель в меню | P1 | `theme/tokens.ts`, `theme/ThemeModeContext.tsx` |
| Q-15 | Адаптивность от 360 px: меню на телефоне в выдвижной панели, шапка экрана в столбец, страница не едет по горизонтали | P1 | `AppLayout.tsx`, `PageHeader.tsx` |
| Q-16 | Accessibility smoke на `axe-core`: нарушения serious и critical роняют сборку; найдено и исправлено одно настоящее — подпись типа активности была недоступна диктору | P1 | `web/src/test/axe.ts`, `accessibility.test.tsx` |
| Q-17 | Ссылка «Перейти к содержимому» первой в порядке обхода клавиатурой | P1 | `AppLayout.tsx` |
| Q-14 | Защита несохранённых изменений в формах клиента и сделки, включая закрытие вкладки | P1 | `web/src/components/useUnsavedChanges.ts` |
| Q-11 | Идемпотентность создания по заголовку `Idempotency-Key`: повтор потерянного запроса возвращает результат первой попытки, а не создаёт дубликат | P1 | `common/idempotency/`, миграция `AddIdempotencyKeys` |
| Q-10 | CI на сервере: проверки и тесты с PostgreSQL, отдельно сборка образов, smoke, нагрузка, копия и восстановление | P1 | `.github/workflows/ci.yml` |

## Платформенный срез от 2026-09-18

| ID | Что добавлено | Приоритет | Где |
|---|---|---|---|
| P-01 | Сквозной идентификатор запроса: заголовок, тело ошибки, контекст логов; внешнее значение принимается только безопасным | P1 | `common/middleware/request-context.middleware.ts`, `common/logging/request-context.ts` |
| P-02 | Структурированные JSON-логи в production с уровнем, контекстом, идентификатором запроса и пользователем | P1 | `common/logging/json.logger.ts` |
| P-03 | Единый контракт ошибок для исключений приложения, ошибок СУБД, ошибок разбора тела и непредвиденных сбоев; текст внутренних ошибок наружу не отдаётся | P1 | `common/filters/all-exceptions.filter.ts`, `common/filters/database-errors.ts` |
| P-04 | Предел размера тела запроса с понятным 413 и принудительным закрытием недочитанного соединения | P1 | `common/app-setup.ts`, `BODY_LIMIT` |
| P-05 | OpenAPI: `/api/docs`, `/api/docs/openapi.json`, файл `docs/openapi.json` и команда его обновления | P1 | `common/openapi.ts`, `src/openapi-export.ts` |
| P-06 | Общая настройка приложения для запуска и тестов: e2e проверяют ту же сборку, что уходит в production | P2 | `common/app-setup.ts`, `test/setup.ts` |

Проверки закреплены в `server/test/platform.e2e-spec.ts`.

## Мультитенантность от 2026-09-18

| ID | Что сделано | Приоритет | Где |
|---|---|---|---|
| T-01 | Таблица `organizations` и принадлежность записи организации | P0 | `organizations/organization.entity.ts`, миграция `1789700000000-AddOrganizations` |
| T-02 | `organization_id` в `users`, `clients`, `contacts`, `deals`, `deal_stage_history`, `activities`, `commercial_offers`, `audit_log` | P0 | сущности модулей |
| T-03 | Миграция существующих данных в организацию по умолчанию, проверенная на непустой базе; откат проверен отдельно | P0 | миграция `AddOrganizations` |
| T-04 | Централизованный tenant scope во всех списках, карточках, дашборде и отчётах | P0 | `common/helpers/tenant-scope.ts` |
| T-05 | Чужая запись отдаётся как отсутствующая (404), а не запрещённая (403) | P0 | сервисы модулей |
| T-06 | Запрет назначать ответственным сотрудника другой организации (внешний ключ этого не ловит) | P0 | `clients`, `deals`, `activities` |
| T-07 | Составные индексы `organization_id + owner/stage/planned_at` под запросы со скоупом | P1 | миграция `AddOrganizations` |
| T-08 | Вторая организация в демонстрационных данных: изоляция проверяется на реальных данных | P1 | `database/seeds/seed.ts` |

Изоляция закреплена 20 проверками в `server/test/tenancy.e2e-spec.ts`:
прямые ссылки на чужие записи, попытки их изменить, попытки сослаться на
них из своих записей, состав списков, отчётов и дашборда.

**Что в мультитенантности осталось незакрытым:** пользователь принадлежит
ровно одной организации (модели membership и переключения организаций
нет), организации заводятся миграцией и seed — API управления ими и
приглашений пока нет. Пока это сознательное ограничение, а не пробел в
изоляции: сама изоляция закрыта и проверена.

## Закрытые P0-риски

- Refresh-токены больше не являются неотзываемыми: добавлены серверные сессии, SHA-256-хеш токена, ротация, обнаружение повторного использования и отзыв при logout.
- Refresh и logout защищены double-submit CSRF-токеном.
- Блокировка пользователя и смена пароля отзывают refresh-сессии.
- Добавлены liveness и readiness endpoints с проверкой PostgreSQL.
- Контейнер API запускается не от root.
- Docker Compose ожидает readiness API и проверяет web.
- Страницы загружаются отдельными чанками; стартовый JS сокращён примерно на две трети.
- React Router обновлён до версии без найденных audit-уязвимостей.
- Уязвимый Multer заменён безопасной версией через корневой override до перехода на следующий major NestJS.

## Открытые P0

- Нет непрерывного архивирования WAL: восстановление возможно только на момент последней копии (RPO — сутки).
- Нет автоматической выгрузки копий во внешнее хранилище: каталог копий отправляется наружу средствами хоста.
- Нет совместимости нового кода со старой схемой на время выкатки: обновление требует короткого простоя.

## Открытые P1

- Главный frontend chunk всё ещё немного превышает бюджет 500 КБ и требует vendor splitting.
- Нет пользовательского импорта, дедупликации, тегов, пользовательских полей и нескольких воронок.
- Нет фоновых заданий для больших экспортов.
- Нет отправки логов во внешний сборщик и метрик.
- Клавиатурная навигация проверена выборочно: сквозного обхода всех экранов и модальных окон не проводилось.
- Тёмная схема проверена на основных экранах; отдельные редкие состояния (формы с ошибками, пустые состояния отчётов) глазами не просматривались.
- Нет браузерных e2e-тестов.
- Optimistic locking введён только для клиентов и сделок; активности, КП и контакты сохраняются по-прежнему «последний выигрывает».

## Открытые P2

- Нет TOTP 2FA, recovery-кодов, приглашений и восстановления пароля.
- Нет каталога товаров и позиций коммерческого предложения.
- Нет центра уведомлений и повторяющихся задач.
- Нет расширенной аналитики и сохранённых представлений.
- Нет экспорта метрик во внешний сборщик (Prometheus): диагностика отдаётся только как JSON администратору.

## Зависимости и лицензии

Production audit не обнаруживает critical/high проблем. Остаётся moderate advisory `uuid`, транзитивно используемый `exceljs`. Уязвимый API генерации UUID не вызывается кодом CRM, но зависимость должна быть заменена или обновлена после появления совместимой версии. Принудительная несовместимая подмена не используется.

До коммерческого релиза требуется автоматическая генерация перечня лицензий и ручная проверка всех файлов, шрифтов и изображений. Нельзя заявлять юридическое соответствие продукта без оценки конкретного развёртывания.

## Архитектурные риски

- Все бизнес-таблицы относятся к одной организации; добавление tenant scope потребует миграции каждой сущности, индексов и полного набора negative e2e.
- Роль хранится непосредственно в пользователе; для нескольких организаций нужна membership-модель.
- Экспорты формируются синхронно и могут занимать HTTP worker.
- Файлы пока представлены ссылкой и не имеют безопасного upload pipeline.
- Аудит маскирует известные чувствительные поля по списку имён; при добавлении новых полей список нужно пополнять, иначе значение попадёт в журнал.

## Первый вертикальный срез

Первый срез включает серверные refresh-сессии, их ротацию и отзыв, health/readiness, безопасную версию Multer и Docker healthchecks. Срез подтверждается production build, 30 frontend-тестами, 84 API e2e и `docker compose config`.

Следующий P0-срез: организация, membership, tenant context, миграция существующих данных и межтенантные e2e для всех сущностей.
