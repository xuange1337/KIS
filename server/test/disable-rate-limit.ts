/**
 * Отключает ограничение частоты запросов для e2e-тестов.
 *
 * Тесты выполняют десятки входов подряд и упирались бы в предел формы входа.
 * Сам предел проверяется отдельным сьютом throttle.e2e-spec.ts, который
 * снимает эту переменную до загрузки AppModule.
 */
process.env.RATE_LIMIT_DISABLED = 'true';
